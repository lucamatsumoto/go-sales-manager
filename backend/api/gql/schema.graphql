scalar Time

type User {
    id: String!
    name: String!
    email: String!
}

type Item {
    id: String!
    name: String!
    owner: String!
    category: String!   
}

type Mutation {
    createAccount(user: User!): User
    createItem(item: Item!): Item
}

type Query {
    
}